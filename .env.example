# Frontend → backend URL for the AI representative.
# Local development (FastAPI running via uvicorn):
VITE_AI_API_URL=http://localhost:8000

# Production (set this in Vercel → Project → Settings → Environment Variables):
# VITE_AI_API_URL=https://your-service.onrender.com
